{
  "name": "docinsight-ai",
  "version": "1.0.0",
  "description": "DocInsight AI - Document Intelligence Platform",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "backend": "cd backend && .\\venv\\Scripts\\activate && python main.py",
    "install-all": "npm install --prefix frontend --legacy-peer-deps && cd backend && python -m pip install -r requirements.txt"
  }
}
